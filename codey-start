#!/data/data/com.termux/files/usr/bin/bash
#
# codey-start - Codey-OS Orchestrator (services + GUI + TUI)
#
# Brings up daemon, Restoricon Core API, Aigentik, Cloudflare tunnel, and GUI
# in background, then drops into the interactive TUI (codeyOS) in foreground.
#

set -e

CODEY_OS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export CODEY_OS_DIR
export CODEY_DIR="$CODEY_OS_DIR"

source "$CODEY_OS_DIR/lib/service_manager.sh"

start_all_services true

echo "  TUI         → launching codeyOS..."
echo

exec bash "$CODEY_OS_DIR/codeyOS" "$@"
